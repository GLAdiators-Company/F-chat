<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FChat" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/themeColor</item>
        <item name="android:statusBarColor">@color/themeColor</item>
        <item name="android:actionBarItemBackground">@color/themeColor</item>
        <item name="android:textColorHint">@color/themeColor</item>
        <item name="android:textColorPrimary">@color/themeColor</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <style name="Theme.FChat" parent="Base.Theme.FChat">

    </style>
    <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="colorOutline">@color/themeColor</item>
    </style>
</resources>